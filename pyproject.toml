[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "llmradar"
version = "0.1.0"
description = "Production radar for LLM apps — capture a baseline, detect when latency, cost, or behavior drifts."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [{ name = "llmradar contributors" }]
keywords = ["llm", "observability", "drift", "monitoring", "openai", "anthropic"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
    "Topic :: System :: Monitoring",
]
dependencies = [
    "numpy>=1.24",
    "scipy>=1.11",
    "typer>=0.12",
    "rich>=13.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[project.scripts]
llmradar = "llmradar.cli:app"

[project.urls]
Homepage = "https://github.com/pallaprolus/llmradar"
Issues = "https://github.com/pallaprolus/llmradar/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/llmradar"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
